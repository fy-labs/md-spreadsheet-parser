name: Publish to NPM

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build-and-publish:
    name: Build and Publish to NPM
    runs-on: ubuntu-latest
    environment:
      name: npm
      url: https://www.npmjs.com/package/md-spreadsheet-parser
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build Python Wheel
        run: uv build

      - name: Install dependencies
        working-directory: packages/npm
        run: npm ci

      - name: Build NPM Package
        working-directory: packages/npm
        run: npm run build

      - name: Test
        working-directory: packages/npm
        run: npm test

      - name: Publish to NPM
        working-directory: packages/npm
        run: npm publish --provenance --access public
